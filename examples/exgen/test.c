#include <stdio.h>

int plus(int a, int b){
    int c;
    result = a + b;
    return c;
}

int test(int x, int y){
    int result;
    puts("test");
    result = plus(x, y);
    return result;
}

int main(){
    int a = 1, b = 2;
    test(a, b);
    return 0;
}
